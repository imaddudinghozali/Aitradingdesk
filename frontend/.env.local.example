# Backend API base URL — accessed from the browser, so must be reachable from client.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Default symbol for dashboard panels
NEXT_PUBLIC_DEFAULT_SYMBOL=XAUUSD
